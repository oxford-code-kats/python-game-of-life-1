python-game-of-life-1
=====================

With Console UI and working tests.

Running it
----------
Run `python ui.py`

This will start, display and run 200 steps of a random board.
To configure, edit the code.

